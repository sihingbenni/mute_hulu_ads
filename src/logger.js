/**
 * log info message with prefix
 * @param {string} msg 
 */
function info(msg = "") {
    console.log("[hulu_mute] " + msg)
}

/**
 * log error message with prefix
 * @param {string} msg 
 */
function error() {
    console.error("[hulu_mute] " + msg)
}